# Copy to .streamlit/secrets.toml for local testing (gitignored).
# On Streamlit Community Cloud, paste the same two lines into
# your app's Settings > Secrets box instead.

SUPABASE_URL = "https://xxxx.supabase.co"
SUPABASE_ANON_KEY = "your-anon-public-key"
